import React from 'react';
import { motion } from 'motion/react';
import { BookOpen, CheckCircle, Layout, Palette, Users, Megaphone, Tablet, GraduationCap, ArrowRight, Send, MessageCircle, Facebook, Instagram } from 'lucide-react';
import Navbar from './components/Navbar';
import Footer from './components/Footer';
import WhatsAppButton from './components/WhatsAppButton';
import ShareButton from './components/ShareButton';
import BlogModal from './components/BlogModal';
import BookModal from './components/BookModal';
import ManuscriptModal from './components/ManuscriptModal';
import Newsletter from './components/Newsletter';
import { SERVICES, BLOG_POSTS, FEATURED_BOOKS } from './constants';
const API_URL = (import.meta.env.VITE_API_URL as string) || 'http://localhost:3001';
const App = () => {
  const [showAllBooks, setShowAllBooks] = React.useState(false);
  const [selectedPost, setSelectedPost] = React.useState<typeof BLOG_POSTS[0] | null>(null);
  const [apiPosts, setApiPosts] = React.useState<typeof BLOG_POSTS>([]);
  const [selectedBook, setSelectedBook] = React.useState<typeof FEATURED_BOOKS[0] | null>(null);
  const [siteContent, setSiteContent] = React.useState<{ hero: { titulo: string; subtitulo: string }; sobre: { texto: string; missao: string; visao: string }; contacto: { morada: string; telefone: string; email: string; facebook: string; instagram: string; whatsapp: string }; } | null>(null);
  const [showAllPosts, setShowAllPosts] = React.useState(false);
  const [showManuscript, setShowManuscript] = React.useState(false);
  const [formData, setFormData] = React.useState({ nome: '', email: '', servico: 'Edicao', mensagem: '' });
  const [formStatus, setFormStatus] = React.useState<'idle' | 'sending' | 'success' | 'error'>('idle');
  React.useEffect(() => {
    const fetchPosts = () => { fetch(`${API_URL}/api/posts`).then(r => r.json()).then(data => { if (Array.isArray(data)) setApiPosts(data); }).catch(() => {}); };
    const fetchSiteContent = () => { fetch(`${API_URL}/api/content`).then(r => r.json()).then(data => { if (data && data.hero && data.sobre && data.contacto) setSiteContent(data); }).catch(() => {}); };
    fetchPosts(); fetchSiteContent();
    const interval = setInterval(() => { fetchPosts(); fetchSiteContent(); }, 30000);
    return () => clearInterval(interval);
  }, []);
  const allPosts = [...apiPosts, ...BLOG_POSTS];
  const visiblePosts = showAllPosts ? allPosts : allPosts.slice(0, 3);
  const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); setFormStatus('sending'); try { const res = await fetch(`${API_URL}/api/contact`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(formData) }); if (res.ok) { setFormStatus('success'); setFormData({ nome: '', email: '', servico: 'Edicao', mensagem: '' }); } else setFormStatus('error'); } catch { setFormStatus('error'); } };
  const iconMap: Record<string, React.ReactNode> = { BookOpen: <BookOpen />, CheckCircle: <CheckCircle />, Layout: <Layout />, Palette: <Palette />, Users: <Users />, Megaphone: <Megaphone />, Tablet: <Tablet />, GraduationCap: <GraduationCap /> };
  return (<div className="min-h-screen"><ManuscriptModal open={showManuscript} onClose={() => setShowManuscript(false)} /><BlogModal post={selectedPost} onClose={() => setSelectedPost(null)} /><BookModal book={selectedBook} onClose={() => setSelectedBook(null)} /><Navbar /><WhatsAppButton /><section id="home" className="relative min-h-screen flex items-center overflow-hidden bg-navy"><div className="absolute inset-0 z-0"><img src="https://images.unsplash.com/photo-1507842217343-583bb7270b66?auto=format&fit=crop&q=80&w=2000" alt="Library" className="w-full h-full object-cover opacity-30" referrerPolicy="no-referrer" /><div className="absolute inset-0 bg-gradient-to-r from-navy via-navy/80 to-transparent"></div></div><div className="relative z-10 max-w-7xl mx-auto px-5 md:px-12 w-full py-32 md:py-0"><motion.div initial={{ opacity: 0, y: 30 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.8 }} className="max-w-3xl"><h1 className="text-3xl sm:text-5xl md:text-7xl font-serif font-bold text-white mb-6 leading-tight">{siteContent?.hero?.titulo ? siteContent.hero.titulo.replace('florescer.', '') : 'A palavra e a semente, a nossa missao e faze-la '}<span className="text-primary-blue">florescer.</span></h1><p className="text-base sm:text-xl text-slate-300 mb-8 leading-relaxed font-light">{siteContent?.hero?.subtitulo ?? 'Apoiamos autores e transformamos palavras em obras publicadas com excelencia, etica e sofisticacao cultural.'}</p><div className="flex flex-col sm:flex-row gap-3"><motion.a href="#contacto" whileHover={{ scale: 1.02 }} className="bg-gold text-navy px-6 py-4 font-bold tracking-widest text-xs rounded-sm flex items-center justify-center gap-2 hover:bg-white transition-all duration-300">PUBLICAR O MEU LIVRO <ArrowRight size={16} /></motion.a><motion.a href="https://wa.me/258834698880" target="_blank" whileHover={{ scale: 1.02 }} className="bg-white/10 text-white border border-white/20 px-6 py-4 font-bold tracking-widest text-xs rounded-sm flex items-center justify-center gap-2 hover:bg-white/20 transition-all duration-300">FALAR NO WHATSAPP <MessageCircle size={16} /></motion.a></div></motion.div></div></section><section id="catalogo" className="bg-white py-16 md:py-24 px-5 md:px-12"><div className="max-w-7xl mx-auto"><div className="mb-16"><span className="text-gold font-bold tracking-widest text-xs mb-4 block">CATALOGO</span><h2 className="text-2xl sm:text-4xl font-serif font-bold text-navy leading-tight">Obras Publicadas e Disponiveis para Aquisicao</h2></div><div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">{(showAllBooks ? FEATURED_BOOKS : FEATURED_BOOKS.slice(0, 4)).map((book, index) => (<motion.div key={index} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ delay: (index % 4) * 0.1 }} viewport={{ once: true }} className="group cursor-pointer" onClick={() => setSelectedBook(book)}><div className="relative overflow-hidden mb-4 aspect-[3/4] shadow-xl"><img src={book.cover} alt={book.title} className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" referrerPolicy="no-referrer" /></div><h3 className="font-serif font-bold text-base text-navy mb-1 leading-snug">{book.title}</h3><p className="text-slate-500 text-xs italic">{book.author}</p></motion.div>))}</div><div className="mt-12 text-center"><button onClick={() => setShowAllBooks(!showAllBooks)} className="inline-flex items-center gap-2 bg-navy text-white px-8 py-4 text-xs font-bold tracking-widest hover:bg-gold transition-all duration-300 rounded-sm">{showAllBooks ? 'VER MENOS' : `VER TODAS AS ${FEATURED_BOOKS.length} OBRAS`}</button></div></div></section><section id="servicos" className="bg-navy py-16 md:py-24 px-5 md:px-12 relative overflow-hidden"><div className="absolute top-0 right-0 w-1/3 h-full bg-white/5 -skew-x-12 translate-x-1/2"></div><div className="max-w-7xl mx-auto relative z-10"><div className="text-center mb-20"><span className="text-gold font-bold tracking-widest text-xs mb-4 block">SOLUCOES EDITORIAIS</span><h2 className="text-2xl sm:text-4xl md:text-5xl font-serif font-bold text-white mb-6">Areas de Actuacao e Servicos</h2></div><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">{SERVICES.map((service, index) => (<motion.div key={index} initial={{ opacity: 0, scale: 0.95 }} whileInView={{ opacity: 1, scale: 1 }} transition={{ delay: index * 0.05 }} viewport={{ once: true }} className="bg-white/5 border border-white/10 p-8 hover:bg-white hover:border-white transition-all duration-500 group"><div className="text-gold mb-6 group-hover:text-navy transition-colors duration-500">{iconMap[service.icon] || <BookOpen />}</div><h4 className="text-white font-serif font-bold text-lg mb-4 group-hover:text-navy transition-colors duration-500">{service.title}</h4><p className="text-slate-400 text-sm leading-relaxed group-hover:text-slate-600 transition-colors duration-500">{service.description}</p></motion.div>))}</div><div className="mt-16 flex flex-col sm:flex-row items-center justify-center gap-4"><motion.a href="#contacto" whileHover={{ scale: 1.05 }} className="w-full sm:w-auto inline-flex items-center justify-center gap-3 bg-gold text-navy px-10 py-5 font-bold tracking-widest text-sm rounded-sm hover:bg-white transition-all duration-300 shadow-2xl">SOLICITAR SERVICO <ArrowRight size={18} /></motion.a><motion.button onClick={() => setShowManuscript(true)} whileHover={{ scale: 1.05 }} className="w-full sm:w-auto inline-flex items-center justify-center gap-3 bg-white/10 border border-white/30 text-white px-10 py-5 font-bold tracking-widest text-sm rounded-sm hover:bg-white/20 transition-all duration-300 shadow-2xl">SUBMETER MANUSCRITO <ArrowRight size={18} /></motion.button></div></div></section><section id="blog" className="bg-white py-16 md:py-24 px-5 md:px-12"><div className="max-w-7xl mx-auto"><div className="mb-12 md:mb-16"><span className="text-gold font-bold tracking-widest text-xs mb-4 block">CONHECIMENTO & CULTURA</span><h2 className="text-2xl sm:text-4xl font-serif font-bold text-navy leading-tight">Blog Vertice Literario</h2></div><div className="grid grid-cols-1 md:grid-cols-3 gap-10">{visiblePosts.map((post, index) => (<motion.article key={post.id} initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} transition={{ delay: index * 0.2 }} viewport={{ once: true }} className="flex flex-col group"><div className="relative overflow-hidden aspect-video mb-6 shadow-lg"><img src={post.image} alt={post.title} className="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110" referrerPolicy="no-referrer" /><div className="absolute top-4 left-4 bg-navy text-white text-[10px] font-bold tracking-widest px-3 py-1 uppercase">{post.date}</div></div><div className="flex items-center gap-2 text-[10px] font-bold text-gold tracking-widest mb-3 uppercase"><Users size={12} /> {post.author}</div><h3 className="text-xl font-serif font-bold text-navy mb-4 group-hover:text-gold transition-colors duration-300 leading-snug">{post.title}</h3><p className="text-slate-500 text-sm leading-relaxed mb-6 flex-grow">{post.excerpt}</p><div className="flex items-center justify-between pt-6 border-t border-slate-100"><button onClick={() => setSelectedPost(post)} className="text-xs font-bold tracking-widest text-navy hover:text-gold transition-colors flex items-center gap-2 uppercase">Ler Mais <ArrowRight size={14} /></button><div className="flex gap-3 text-slate-400"><a href="https://www.facebook.com/profile.php?id=61569927223809" target="_blank" rel="noopener noreferrer" className="hover:text-gold transition-colors"><Facebook size={16} /></a><a href="https://www.instagram.com/edit.oraverticeliterario/" target="_blank" rel="noopener noreferrer" className="hover:text-gold transition-colors"><Instagram size={16} /></a><ShareButton url={`${window.location.origin}/#blog`} title={post.title} /></div></div></motion.article>))}</div>{allPosts.length > 3 && (<div className="mt-12 text-center"><button onClick={() => setShowAllPosts(!showAllPosts)} className="inline-flex items-center gap-2 bg-navy text-white px-8 py-4 text-xs font-bold tracking-widest hover:bg-gold transition-all duration-300 rounded-sm">{showAllPosts ? 'VER MENOS' : `VER TODOS (${allPosts.length})`}</button></div>)}<div className="mt-16"><Newsletter /></div></div></section><section id="contacto" className="bg-slate-50 py-16 md:py-24 px-5 md:px-12"><div className="max-w-7xl mx-auto"><div className="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20"><motion.div initial={{ opacity: 0, x: -30 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }}><span className="text-gold font-bold tracking-widest text-xs mb-4 block">CONTACTO</span><h2 className="text-2xl sm:text-4xl font-serif font-bold text-navy mb-6 leading-tight">Vamos Dar Vida a Sua Obra Literaria</h2><div className="space-y-8"><div className="flex gap-6"><div className="w-14 h-14 bg-white shadow-md flex items-center justify-center shrink-0"><Send className="text-gold" size={24} /></div><div><h4 className="font-serif font-bold text-navy text-lg mb-1">E-mail</h4><p className="text-slate-500">editoraverticeliterario@gmail.com</p></div></div><div className="flex gap-6"><div className="w-14 h-14 bg-white shadow-md flex items-center justify-center shrink-0"><MessageCircle className="text-gold" size={24} /></div><div><h4 className="font-serif font-bold text-navy text-lg mb-1">WhatsApp / Telefone</h4><p className="text-slate-500">(+258) 83 46 98 880</p></div></div><div className="flex gap-6"><div className="w-14 h-14 bg-white shadow-md flex items-center justify-center shrink-0"><BookOpen className="text-gold" size={24} /></div><div><h4 className="font-serif font-bold text-navy text-lg mb-1">Localizacao</h4><p className="text-slate-500">Cidade de Tete, Paragem Juventude, junto ao Mercado Cambinde. Estrada Nacional n7, Mocambique.</p></div></div></div></motion.div><motion.div initial={{ opacity: 0, x: 30 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} className="bg-white p-5 sm:p-8 md:p-12 shadow-2xl rounded-sm"><form className="space-y-6" onSubmit={handleSubmit}><div className="grid grid-cols-1 md:grid-cols-2 gap-6"><div className="space-y-2"><label className="text-[10px] font-bold tracking-widest text-slate-400 uppercase">Nome Completo</label><input type="text" required value={formData.nome} onChange={e => setFormData({...formData, nome: e.target.value})} className="w-full bg-slate-50 border-b border-slate-200 p-4 focus:border-gold outline-none transition-colors" placeholder="Ex: Joao Silva" /></div><div className="space-y-2"><label className="text-[10px] font-bold tracking-widest text-slate-400 uppercase">E-mail</label><input type="email" required value={formData.email} onChange={e => setFormData({...formData, email: e.target.value})} className="w-full bg-slate-50 border-b border-slate-200 p-4 focus:border-gold outline-none transition-colors" placeholder="exemplo@email.com" /></div></div><div className="space-y-2"><label className="text-[10px] font-bold tracking-widest text-slate-400 uppercase">Servico de Interesse</label><select value={formData.servico} onChange={e => setFormData({...formData, servico: e.target.value})} className="w-full bg-slate-50 border-b border-slate-200 p-4 focus:border-gold outline-none transition-colors appearance-none"><option>Edicao e Publicacao</option><option>Revisao Textual</option><option>Design de Capa</option><option>Outros</option></select></div><div className="space-y-2"><label className="text-[10px] font-bold tracking-widest text-slate-400 uppercase">Mensagem</label><textarea rows={4} required value={formData.mensagem} onChange={e => setFormData({...formData, mensagem: e.target.value})} className="w-full bg-slate-50 border-b border-slate-200 p-4 focus:border-gold outline-none transition-colors resize-none" placeholder="Conte-nos sobre o seu projeto..."></textarea></div><button type="submit" disabled={formStatus === 'sending'} className="w-full bg-navy text-white py-5 font-bold tracking-widest text-xs hover:bg-gold transition-all duration-300 shadow-lg uppercase disabled:opacity-60">{formStatus === 'sending' ? 'A ENVIAR...' : 'Enviar Mensagem'}</button>{formStatus === 'success' && <p className="text-center text-sm text-green-600 font-medium">Mensagem enviada com sucesso!</p>}{formStatus === 'error' && <p className="text-center text-sm text-red-500 font-medium">Erro ao enviar. Tente novamente.</p>}</form></motion.div></div></div></section><Footer contacto={siteContent?.contacto} /></div>);};
export default App;
